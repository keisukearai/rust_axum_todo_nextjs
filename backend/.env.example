# 必須: 別プロセスで動く PostgreSQL への接続先
DATABASE_URL=postgres://YOUR_USER@localhost:5432/todo_next

# 任意（既定値をコメントで示す）
# HOST=127.0.0.1
# PORT=8080
# DATABASE_MAX_CONNECTIONS=5
# CORS_ORIGIN=http://localhost:3000
# RUST_LOG=todo_backend=debug,tower_http=info,sqlx=warn
